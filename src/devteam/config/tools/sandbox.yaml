runtimes:
    python:
        image: 'python:3.12-pytest'
        command: 'bash -c "PYTHONPATH=/workspace pytest -v"'
    node:
        image: 'node:20-slim'
        command: 'bash -c "npm install --silent && npm test"'
